import { Box, Typography } from '@mui/material';

function Stats() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4" gutterBottom>Stats</Typography>
      <Typography>This page will show analytics about your blog posts and user interactions (feature coming soon).</Typography>
    </Box>
  );
}

export default Stats;
